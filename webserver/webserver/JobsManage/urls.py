#!/usr/bin/env python
# -*- coding:utf-8 -*-

from django.conf.urls import include, url
from . import views

urlpatterns = [
    # url(r"^jobs/template/lists/$", views.JobsTemplateList.as_view()),
    # url(r"^jobs/template/add/$", views.JobsTemplateAdd.as_view()),
    # url(r"^jobs/template/delete/$", views.JobsTemplateDelete.as_view()),

    # url(r"^jobs/notify/lists/$", views.JobsNotifyList.as_view()),
    # url(r"^jobs/notify/add/$", views.JobsNotifyAdd.as_view()),
    # url(r"^jobs/notify/delete/$", views.JobsNotifyDelete.as_view()),
]
